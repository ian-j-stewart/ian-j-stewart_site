---
import { getThemeLabel } from '../lib/themeLabels';

interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<ul class="tag-list" aria-label="Topics">
  {tags.map((tag) => (
    <li>{getThemeLabel(tag)}</li>
  ))}
</ul>
