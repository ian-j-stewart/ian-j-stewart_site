---
interface Props {
  href: string;
  class?: string;
}

const { href, class: className = '' } = Astro.props;
const isExternal = href.startsWith('http');
---

<a href={href} class={`external ${className}`} rel={isExternal ? 'external noopener' : undefined}>
  <slot />
</a>
